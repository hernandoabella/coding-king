import React from "react";

const SideBar = () => {
  return (
    <div
      className="bg-white h-full fixed p-10 left-side-bar border-r"
    >
      Books
    </div>
  );
};

export default SideBar;
